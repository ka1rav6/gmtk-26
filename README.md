# gtmk-26

# FOR GENERIC ENEMY ANIMATION

The animate function has run, jump and idle. These are directly introduced in physics function, so if they are not being used for a specific character, just handle them with pass.
